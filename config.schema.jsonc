{
    /* Salt string to use for passwords */
    "salt": "",

    /* Whether the server is running in a debug environment */
    "debug": false,

    /* If debugging is enabled, enable this to enable the rate limiter
       for debugging purposes. */
    "debugRateLimit": false,

    /* Require invite keys for signup, and require login to view
       pages. */
    "inviteKeys": false
}