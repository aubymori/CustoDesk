RewriteEngine On

# Allow static assets to go through:
RewriteCond %{REQUEST_URI} ^/(s|user_avatars|user_bgs|user_files)/
RewriteCond %{DOCUMENT_ROOT}%{REQUEST_URI} -f
RewriteRule ^ - [L]

# Redirect everything else to index.php
RewriteRule . index.php [L]